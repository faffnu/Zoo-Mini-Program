<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.FavorMapper">

    <select id="selectAll" resultType="com.example.entity.Favor">
        select `favor`.*, `user`.name as user, `animal`.`name` as animal from `favor`
		left join `user` on `favor`.`user_id` = `user`.id
		left join `animal` on `favor`.`animal_id` = `animal`.id

        <where>
			<if test="user != null and user != ''">
				and `user`.name like concat ('%', #{user}, '%')
			</if>
			<if test="animal != null and animal != ''">
				and `animal`.`name` like concat ('%', #{animal}, '%')
			</if>
            <if test="animalId != null and animalId != ''">
                and `animal`.`id` = #{animalId}
            </if>
            <if test="userId != null and userId != ''">
                and `user`.`id` = #{userId}
            </if>

        </where>
    </select>

    <select id="selectByUserId" resultType="com.example.entity.Favor">
        select `favor`.*, `animal`.`id` as animalId,`animal`.`pic_url` as picUrl,`animal`.`name` as animal from `favor`
           left join `animal` on `favor`.`animal_id` = `animal`.id
        where `favor`.`user_id` = #{userId}
    </select>

    <select id="selectById" resultType="com.example.entity.Favor">
        select * from favor where id = #{id}
    </select>

    <delete id="deleteById">
        delete from favor where  id = #{id}
    </delete>


    <delete id="deleteByUserIdAndAnimalId">
        delete from favor where  user_id = #{userId} and animal_id = #{animalId}
    </delete>



</mapper>